{"ast":null,"code":"var ContentComponent_1;\nimport { __decorate } from \"tslib\";\nimport { Component, inject } from '@angular/core';\nimport { MatDialog } from '@angular/material/dialog';\nimport { Subject, tap, switchMap, animationFrames, takeUntil, timer, catchError, of, take, finalize } from 'rxjs';\nimport { ConfirmDialogComponent } from '../shared/components/confirm-dialog/confirm-dialog.component';\nlet ContentComponent = ContentComponent_1 = class ContentComponent {\n  constructor(contentService, interpolationService, loadingService) {\n    this.contentService = contentService;\n    this.interpolationService = interpolationService;\n    this.loadingService = loadingService;\n    this.dialog = inject(MatDialog);\n    this.destroy$ = new Subject();\n    this.play$ = new Subject();\n    this.stop$ = new Subject();\n    this.inactivity$ = new Subject();\n    this.isPlayingGif = false;\n    this.selectedImageIds = []; // User selected image Ids\n    this.firstSelectedImage = null;\n    this.secondSelectedImage = null;\n    this.interpolationFrames = [];\n    this.interpolationState = null;\n    this.currentSliderValue = 50;\n  }\n  ngOnInit() {\n    this.setupImages();\n    this.setupAutoPlay();\n    this.setupAnimation();\n  }\n  ngOnDestroy() {\n    this.destroy$.next();\n    this.destroy$.complete();\n  }\n  // ----------------------------\n  // Images\n  // ----------------------------\n  setupImages() {\n    const selectedDefaultIds = ['0', '1']; // TODO: Update logic\n    this.fetchSelectedImages(selectedDefaultIds, ContentComponent_1.DEFAULT_FRAMES);\n  }\n  // ----------------------------\n  // Animation\n  // ----------------------------\n  setupAnimation() {\n    this.play$.pipe(tap(() => this.isPlayingGif = true), switchMap(() => animationFrames().pipe(takeUntil(this.stop$), takeUntil(this.destroy$)))).subscribe(({\n      elapsed\n    }) => {\n      if (!this.interpolationFrames.length) return;\n      const progress = elapsed % ContentComponent_1.ANIMATION_DURATION_MS / ContentComponent_1.ANIMATION_DURATION_MS;\n      const index = Math.floor(progress * this.interpolationFrames.length);\n      this.interpolationState = this.interpolationFrames[index];\n    });\n    this.stop$.pipe(takeUntil(this.destroy$)).subscribe(() => this.isPlayingGif = false);\n  }\n  // ----------------------------\n  // Auto-play on inactivity\n  // ----------------------------\n  setupAutoPlay() {\n    this.inactivity$.pipe(switchMap(() => timer(ContentComponent_1.AUTO_PLAY_DELAY_MS)), takeUntil(this.destroy$)).subscribe(() => this.play$.next());\n  }\n  // ----------------------------\n  // Public UI handlers\n  // ----------------------------\n  playGif() {\n    this.play$.next();\n  }\n  stopGif() {\n    this.stop$.next();\n    this.inactivity$.next();\n    this.isPlayingGif = false;\n  }\n  onSliderChange(value) {\n    // Set new slider value\n    this.currentSliderValue = value;\n    this.computeInterpolation(value);\n    // Stop GiF play\n    this.stopGif();\n  }\n  /**\n   * Open dialog for image selection.\n   */\n  openDialog() {\n    // Stop GiF play\n    this.stopGif();\n    // Open Dialog and Listen to Close\n    const dialogRef = this.dialog.open(ConfirmDialogComponent, {\n      maxWidth: '90vw',\n      disableClose: true,\n      data: {},\n      panelClass: 'auto-width-dialog'\n    });\n    dialogRef.afterClosed().pipe(take(1)).subscribe(result => {\n      if (!result) return;\n      const {\n        selectedImages\n      } = result;\n      this.fetchSelectedImages(selectedImages, ContentComponent_1.DEFAULT_FRAMES);\n    });\n  }\n  fetchSelectedImages(selectedIds, numberOfFrames) {\n    this.loadingService.startLoading();\n    this.contentService.loadSelectedImages(selectedIds).pipe(tap(data => {\n      this.firstSelectedImage = data[0] ?? null;\n      this.secondSelectedImage = data[1] ?? null;\n    }), switchMap(() => this.interpolationService.loadInterpolationFrames(selectedIds, numberOfFrames)), tap(frames => {\n      this.interpolationFrames = frames;\n      this.computeInterpolation(this.currentSliderValue);\n    }), catchError(err => {\n      console.error('Error loading images or frames', err);\n      return of([]);\n    }), finalize(() => {\n      this.loadingService.stopLoading();\n    }), takeUntil(this.destroy$)).subscribe();\n  }\n  computeInterpolation(sliderValue) {\n    this.interpolationState = this.interpolationService.getInterpolatedFrame(this.interpolationFrames, sliderValue, ContentComponent_1.SLIDER_MIN, ContentComponent_1.SLIDER_MAX);\n  }\n};\nContentComponent.SLIDER_MIN = 0;\nContentComponent.SLIDER_MAX = 100;\nContentComponent.DEFAULT_FRAMES = 20;\nContentComponent.AUTO_PLAY_DELAY_MS = 6000;\nContentComponent.ANIMATION_DURATION_MS = 1500;\nContentComponent = ContentComponent_1 = __decorate([Component({\n  selector: 'app-content',\n  templateUrl: './content.component.html',\n  styleUrls: ['./content.component.sass']\n})], ContentComponent);\nexport { ContentComponent };","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}